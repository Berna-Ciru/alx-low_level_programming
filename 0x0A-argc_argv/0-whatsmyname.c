#include <stdio.h>
#include "main.h"

/**
 * main - prtins name of the program
 * @argv: the argument vector
 * @argc: the argument count
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
